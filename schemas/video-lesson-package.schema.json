{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "VideoLessonPackage",
  "type": "object",
  "required": [
    "lesson_id",
    "source_id",
    "lesson_topic",
    "action",
    "family_id",
    "taxonomy_path",
    "semantic_review_status",
    "completeness",
    "review_status",
    "episode",
    "stages"
  ],
  "properties": {
    "lesson_id": { "type": "string", "minLength": 1 },
    "source_id": { "type": "string", "minLength": 1 },
    "lesson_topic": { "type": "string", "minLength": 1 },
    "action": { "type": "string", "minLength": 1 },
    "family_id": { "type": "string", "minLength": 1 },
    "taxonomy_path": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "semantic_review_status": { "enum": ["agent_reviewed", "model_candidate"] },
    "completeness": {
      "enum": [
        "complete_demonstration",
        "partial_demonstration",
        "static_explanation",
        "concept_only"
      ]
    },
    "review_status": { "enum": ["agent_reviewed", "model_candidate"] },
    "demonstrator_role": { "enum": ["coach", "learner", "unknown"] },
    "example_polarity": {
      "enum": ["correct", "incorrect", "corrective_attempt", "unknown"]
    },
    "context_review_status": {
      "enum": ["agent_reviewed", "model_candidate", "not_reviewed"]
    },
    "context_start_seconds": {
      "type": "number",
      "minimum": 0,
      "description": "Start of reviewed lesson context; cross-field validation requires at least 20 seconds before the action."
    },
    "context_end_seconds": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "End of reviewed lesson context; cross-field validation requires at least 20 seconds after the action."
    },
    "context_evidence": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "confidence": { "enum": ["low", "medium", "high"] },
    "teaching_summary": { "type": "string" },
    "framework_ids": { "type": "array", "items": { "type": "string" } },
    "episode": {
      "type": "object",
      "required": ["start_seconds", "end_seconds"],
      "properties": {
        "start_seconds": { "type": "number", "minimum": 0 },
        "end_seconds": { "type": "number", "exclusiveMinimum": 0 },
        "clip_start_seconds": { "type": "number", "minimum": 0 },
        "clip_end_seconds": { "type": "number", "exclusiveMinimum": 0 }
      },
      "additionalProperties": false
    },
    "stages": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "stage_id",
          "label",
          "phase",
          "start_seconds",
          "anchor_seconds",
          "end_seconds"
        ],
        "properties": {
          "stage_id": { "type": "string", "minLength": 1 },
          "label": { "type": "string", "minLength": 1 },
          "phase": {
            "enum": [
              "preparation",
              "start",
              "arrival",
              "top_elbow",
              "contact_window",
              "follow_through",
              "recovery"
            ]
          },
          "start_seconds": { "type": "number", "minimum": 0 },
          "anchor_seconds": { "type": "number", "minimum": 0 },
          "end_seconds": { "type": "number", "exclusiveMinimum": 0 },
          "confidence": { "enum": ["low", "medium", "high"] },
          "teaching_use": { "type": "string" },
          "teaching_points": { "type": "array", "items": { "type": "string" } },
          "visible_facts": { "type": "array", "items": { "type": "string" } },
          "limitations": { "type": "array", "items": { "type": "string" } },
          "framework_ids": { "type": "array", "items": { "type": "string" } }
        },
        "additionalProperties": false
      }
    },
    "limitations": { "type": "array", "items": { "type": "string" } }
  },
  "allOf": [
    {
      "if": {
        "properties": { "completeness": { "const": "complete_demonstration" } },
        "required": ["completeness"]
      },
      "then": {
        "required": [
          "demonstrator_role",
          "example_polarity",
          "context_review_status",
          "context_start_seconds",
          "context_end_seconds",
          "context_evidence"
        ],
        "properties": {
          "demonstrator_role": { "const": "coach" },
          "example_polarity": { "const": "correct" },
          "context_review_status": { "const": "agent_reviewed" },
          "stages": {
            "minItems": 7,
            "allOf": [
              { "contains": { "properties": { "phase": { "const": "preparation" } }, "required": ["phase"] } },
              { "contains": { "properties": { "phase": { "const": "start" } }, "required": ["phase"] } },
              { "contains": { "properties": { "phase": { "const": "arrival" } }, "required": ["phase"] } },
              { "contains": { "properties": { "phase": { "const": "top_elbow" } }, "required": ["phase"] } },
              { "contains": { "properties": { "phase": { "const": "contact_window" } }, "required": ["phase"] } },
              { "contains": { "properties": { "phase": { "const": "follow_through" } }, "required": ["phase"] } },
              { "contains": { "properties": { "phase": { "const": "recovery" } }, "required": ["phase"] } }
            ]
          }
        }
      }
    }
  ],
  "additionalProperties": false
}
